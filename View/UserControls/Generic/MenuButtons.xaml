<UserControl x:Class="League_Analyser.View.UserControls.MenuButtons"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:local="clr-namespace:League_Analyser.View.UserControls"
        xmlns:global="clr-namespace:League_Analyser"
        HorizontalAlignment="Center" 
        VerticalAlignment="Center"
        Name="menu"  Height="75">

    <UserControl.Resources>
        <Style x:Key="buttonMenu" TargetType="Button">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" >
                <Setter.Value>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FF6EB3FF"/>
                        <GradientStop Color="#FF021AC1" Offset="0.5"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Width" Value="145" />
            <Setter Property="Height" Value="75" />
            <Setter Property="Margin" Value="0,0,2.5,0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" >
                        <Setter.Value>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#FF67C8FF"/>
                                <GradientStop Color="#FF027FE8" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="DarkBlue" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel Name="menuStackPanel" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="button_stats" Style="{StaticResource buttonMenu}" 
                    Content="{x:Static global:Messages.menu_statistics}" Click="Button_Click"/>
            
            <Button Name="button_matchHistory" Style="{StaticResource buttonMenu}" 
                    Content="{x:Static global:Messages.menu_matchHistory}" Click="Button_Click"/>
            
            <Button Name="button_settings" Style="{StaticResource buttonMenu}" 
                    Content="{x:Static global:Messages.menu_settings}" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
